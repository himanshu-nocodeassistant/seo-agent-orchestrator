# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Local runtime state (created on first server start)
kanban.db
kanban.db-*
kanban.staging.db
*.sqlite3

# Secrets — never commit service-account keys
google-sa-credentials/

# Claude
.claude/backups/

# Generated agent views (regenerated from memory/seo-*.json on every run)
.claude/seo-changes-log.md
.claude/seo-learnings.md

# DataForSEO pipeline output
# raw/ and manifests/ are bulky, transient dumps of paid API calls — keep the
# folders in git, ignore their contents. compiled/ rollups stay tracked because
# they are the artifacts the agent actually reads.
dataforseo/raw/*
!dataforseo/raw/.gitkeep
dataforseo/manifests/*
!dataforseo/manifests/.gitkeep
